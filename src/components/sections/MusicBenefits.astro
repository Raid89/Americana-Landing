---
const musicBenefits = [
  {
    icon: "🧠",
    title: "Desarrollo Cognitivo",
    description: "La música estimula el desarrollo neuronal, mejora la memoria y potencia la capacidad de concentración y aprendizaje.",
    color: "#FF6363"
  },
  {
    icon: "💫",
    title: "Expresión Creativa",
    description: "Aprende a expresar emociones y sentimientos a través de la música, desarrollando tu creatividad artística.",
    color: "#66B2FF"
  },
  {
    icon: "🤝",
    title: "Habilidades Sociales",
    description: "Tocar en grupo fomenta el trabajo en equipo, la comunicación y la construcción de relaciones duraderas.",
    color: "#FFB366"
  },
  {
    icon: "😌",
    title: "Bienestar Emocional",
    description: "La música reduce el estrés, mejora el estado de ánimo y proporciona una vía saludable para el manejo emocional.",
    color: "#A366FF"
  },
  {
    icon: "🎯",
    title: "Disciplina y Constancia",
    description: "El aprendizaje musical desarrolla la disciplina, paciencia y perseverancia necesarias para el crecimiento personal.",
    color: "#66FFB2"
  },
  {
    icon: "🌟",
    title: "Confianza Personal",
    description: "Dominar un instrumento aumenta la autoestima, la confianza en uno mismo y la sensación de logro personal.",
    color: "#FF66B2"
  }
];
---

<section id="music-benefits" class="music-benefits" data-animate="benefits">
  <div class="benefits-header" data-grid="header">
    <h2>¿Por Qué Aprender Música?</h2>
    <p>Descubre los increíbles beneficios que la música aporta a tu vida</p>
  </div>

  <div class="benefits-grid">
    {musicBenefits.map((benefit, index) => (
      <div 
        class="benefit-card" 
        data-benefit="card"
        style={`--accent-color: ${benefit.color}`}
      >
        <div class="benefit-icon">
          <span>{benefit.icon}</span>
        </div>
        <div class="benefit-content">
          <h3>{benefit.title}</h3>
          <p>{benefit.description}</p>
        </div>
        <div class="benefit-decoration"></div>
      </div>
    ))}
  </div>

  <div class="benefits-cta" data-grid="cta">
    <div class="cta-content">
      <h3>🎵 ¿Listo para comenzar tu viaje musical?</h3>
      <p>No importa tu edad o nivel de experiencia. En Americana Academy, todos pueden aprender música.</p>
      <div class="cta-buttons">
        <a href="#contactSection" class="cta-primary">
          <span>🎹</span>
          Reservar Clase de Prueba
        </a>
        <a href="#music-instruments" class="cta-secondary">
          <span>🎸</span>
          Ver Instrumentos
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  @import "@styles/animations.css";

  .music-benefits {
    padding: 120px 100px;
    background: linear-gradient(180deg, #F8F6F5 0%, #FDFBFA 50%, #F8F6F5 100%);
    position: relative;
    overflow: hidden;
  }

  .music-benefits::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="10" cy="10" r="1" fill="%23FF6363" opacity="0.1"/><circle cx="90" cy="20" r="1.5" fill="%2366B2FF" opacity="0.1"/><circle cx="20" cy="80" r="1.2" fill="%23FFB366" opacity="0.1"/><circle cx="80" cy="90" r="0.8" fill="%23A366FF" opacity="0.1"/></svg>') repeat;
    pointer-events: none;
  }

  .benefits-header {
    text-align: center;
    margin-bottom: 80px;
    position: relative;
    z-index: 1;

    h2 {
      font-size: 64px;
      font-weight: 600;
      margin: 0 0 20px 0;
      background: linear-gradient(135deg, #212529, #FF6363);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    p {
      font-size: 20px;
      color: #666;
      margin: 0;
      font-weight: 500;
    }
  }

  .benefits-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 40px;
    margin-bottom: 80px;
    position: relative;
    z-index: 1;
  }

  .benefit-card {
    background: white;
    border-radius: 25px;
    padding: 40px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.08);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    border: 2px solid transparent;
    position: relative;
    overflow: hidden;
    animation-delay: var(--delay);
  }

  .benefit-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: var(--accent-color);
  }

  .benefit-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.12);
    border-color: var(--accent-color);
  }

  .benefit-icon {
    width: 80px;
    height: 80px;
    background: var(--accent-color);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 25px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    transition: transform 0.3s ease;

    span {
      font-size: 40px;
      filter: brightness(1.2);
    }
  }

  .benefit-card:hover .benefit-icon {
    transform: scale(1.1) rotate(5deg);
  }

  .benefit-content {
    h3 {
      font-size: 24px;
      font-weight: 600;
      margin: 0 0 15px 0;
      color: #212529;
    }

    p {
      font-size: 16px;
      line-height: 26px;
      color: #666;
      margin: 0;
    }
  }

  .benefit-decoration {
    position: absolute;
    bottom: -10px;
    right: -10px;
    width: 60px;
    height: 60px;
    background: var(--accent-color);
    border-radius: 50%;
    opacity: 0.1;
    transition: transform 0.3s ease;
  }

  .benefit-card:hover .benefit-decoration {
    transform: scale(1.2);
  }

  .benefits-cta {
    text-align: center;
    position: relative;
    z-index: 1;
  }

  .cta-content {
    background: linear-gradient(135deg, #FF6363, #FF8282);
    color: white;
    padding: 60px;
    border-radius: 30px;
    box-shadow: 0 20px 40px rgba(255, 99, 99, 0.2);
    position: relative;
    overflow: hidden;

    h3 {
      font-size: 36px;
      font-weight: 600;
      margin: 0 0 20px 0;
    }

    p {
      font-size: 18px;
      line-height: 28px;
      margin: 0 0 40px 0;
      opacity: 0.95;
    }
  }

  .cta-content::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><path d="M20,50 Q30,30 40,50 T60,50" stroke="rgba(255,255,255,0.1)" stroke-width="2" fill="none"/><circle cx="70" cy="30" r="2" fill="rgba(255,255,255,0.1)"/></svg>') repeat;
    pointer-events: none;
  }

  .cta-buttons {
    display: flex;
    gap: 20px;
    justify-content: center;
    flex-wrap: wrap;
    position: relative;
    z-index: 1;
  }

  .cta-buttons {
    display: flex;
    gap: 20px;
    margin-top: 30px;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
  }

  .cta-primary, .cta-secondary {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 15px 30px;
    border-radius: 25px;
    font-size: 16px;
    font-weight: 600;
    text-decoration: none;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    white-space: nowrap;
    min-width: fit-content;
    flex-shrink: 0;
  }

  .cta-primary {
    background: white;
    color: #FF6363;
    box-shadow: 0 5px 15px rgba(255, 255, 255, 0.3);
  }

  .cta-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 25px rgba(255, 255, 255, 0.4);
  }

  .cta-secondary {
    background: rgba(255, 255, 255, 0.2);
    color: white;
    border: 2px solid rgba(255, 255, 255, 0.3);
  }

  .cta-secondary:hover {
    transform: translateY(-3px);
    background: rgba(255, 255, 255, 0.3);
    border-color: rgba(255, 255, 255, 0.5);
  }

  @media screen and (max-width: 1600px) {
    .music-benefits {
      padding: 100px 60px;
    }

    .benefits-grid {
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: 30px;
    }
  }

  @media screen and (max-width: 1300px) {
    .music-benefits {
      padding: 80px 40px;
    }

    .benefits-header h2 {
      font-size: 48px;
    }

    .benefits-grid {
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 25px;
    }

    .benefit-card {
      padding: 30px;
    }
  }

  @media screen and (max-width: 900px) {
    .music-benefits {
      padding: 60px 30px;
    }

    .benefits-header h2 {
      font-size: 36px;
    }

    .benefits-header p {
      font-size: 18px;
    }

    .benefits-grid {
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 20px;
    }
  }

  @media screen and (max-width: 900px) {
    .music-benefits {
      padding: 60px 30px;
    }

    .benefits-header h2 {
      font-size: 36px;
    }

    .benefits-grid {
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    }

    .cta-buttons {
      gap: 15px;
    }

    .cta-primary, .cta-secondary {
      padding: 14px 25px;
      font-size: 15px;
    }
  }

  @media screen and (max-width: 768px) {
    .music-benefits {
      padding: 60px 20px;
    }

    .benefits-header h2 {
      font-size: 32px;
    }

    .benefits-header p {
      font-size: 16px;
    }

    .benefits-grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .benefit-card {
      padding: 25px;
    }

    .benefit-content h3 {
      font-size: 22px;
    }

    .benefit-description {
      font-size: 15px;
    }

    .cta-content {
      padding: 40px 30px !important;
    }

    .cta-content h3 {
      font-size: 28px;
    }

    .cta-content p {
      font-size: 16px;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: center;
      gap: 12px;
    }

    .cta-primary, .cta-secondary {
      width: 100% !important;
      max-width: 280px !important;
      justify-content: center !important;
      flex-shrink: 0;
    }
  }

  @media screen and (max-width: 480px) {
    .music-benefits {
      padding: 40px 15px;
    }

    .benefits-header h2 {
      font-size: 28px;
    }

    .benefit-card {
      padding: 20px;
    }

    .benefit-icon {
      width: 60px;
      height: 60px;
      
      span {
        font-size: 30px;
      }
    }

    .cta-content {
      padding: 30px 20px !important;
    }

    .cta-content h3 {
      font-size: 24px;
    }

    .cta-buttons {
      gap: 10px;
    }

    .cta-primary, .cta-secondary {
      width: 100% !important;
      max-width: 260px !important;
      padding: 12px 20px !important;
      font-size: 14px !important;
    }
  }
</style>
